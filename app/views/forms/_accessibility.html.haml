%div.inst-box
  %header
    %strong
      Accessibility Options
    %p.desc
      Which accessibility amenities for the location are available?
    - accessibility = @location[:accessibility] || ""
  %fieldset#accessibility
    - accessibility_options.each do |a|
      %label
        = check_box_tag "accessibility_#{a[1]}", a[1], accessibility.include?(a[0]) ? true : false, :name => "accessibility_options[]"
        = a[0]